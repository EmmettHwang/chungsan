# 🔐 권한 관리 시스템 개선 방안

## 📊 현재 시스템 분석

### 현재 구조
```
instructor_codes 테이블
├── code (강사 코드)
├── name (강사명)
├── type (강사 타입)
├── permissions (JSON) - 구버전
├── menu_permissions (TEXT/JSON) - 신버전
├── default_screen
└── dashboard_charts
```

### 현재 문제점
1. **권한 구조 혼재**: `permissions` (구버전)와 `menu_permissions` (신버전) 두 시스템이 공존
2. **메뉴 중심 권한**: 메뉴 접근만 제어, 세밀한 CRUD 권한 부족
3. **역할 기반 부재**: `instructor_type`만 있고 명확한 Role 개념 없음
4. **권한 상속 없음**: 권한을 일일이 설정해야 함
5. **감사 로그 부족**: 누가 언제 무엇을 했는지 추적 어려움

---

## 🎯 개선 방향 제안

### 방안 1: **RBAC (Role-Based Access Control) 도입** ⭐ 추천
역할 기반 권한 관리로 업그레이드

#### 장점
- ✅ 역할별로 권한을 그룹화하여 관리 효율 증대
- ✅ 새로운 사용자 추가 시 역할만 부여하면 됨
- ✅ 권한 변경 시 역할 하나만 수정하면 모든 사용자에 적용
- ✅ 산업 표준으로 검증된 방식

#### 구조
```sql
-- 역할 테이블
roles (
  id INT PRIMARY KEY,
  name VARCHAR(50) UNIQUE,      -- admin, instructor, assistant, viewer
  display_name VARCHAR(100),     -- 관리자, 강사, 보조강사, 조회자
  description TEXT,
  priority INT,                  -- 우선순위 (높을수록 강력)
  created_at DATETIME
)

-- 권한 테이블
permissions (
  id INT PRIMARY KEY,
  resource VARCHAR(50),          -- students, courses, counseling, etc
  action VARCHAR(20),            -- create, read, update, delete, export
  display_name VARCHAR(100),
  description TEXT
)

-- 역할-권한 매핑
role_permissions (
  role_id INT,
  permission_id INT,
  PRIMARY KEY (role_id, permission_id)
)

-- 사용자-역할 매핑
user_roles (
  instructor_code VARCHAR(50),
  role_id INT,
  granted_by VARCHAR(50),        -- 누가 부여했는지
  granted_at DATETIME,
  expires_at DATETIME,            -- 권한 만료일 (선택)
  PRIMARY KEY (instructor_code, role_id)
)
```

#### 역할 예시
```javascript
roles = [
  {
    name: 'super_admin',
    display_name: '최고 관리자',
    permissions: ['*'] // 모든 권한
  },
  {
    name: 'admin',
    display_name: '관리자',
    permissions: [
      'students:*',
      'courses:*',
      'counseling:*',
      'instructors:read,update'
    ]
  },
  {
    name: 'instructor',
    display_name: '강사',
    permissions: [
      'students:read,update',
      'courses:read',
      'counseling:create,read,update',
      'dashboard:read'
    ]
  },
  {
    name: 'assistant',
    display_name: '보조강사',
    permissions: [
      'students:read',
      'courses:read',
      'counseling:read'
    ]
  }
]
```

#### 구현 우선순위
1. **Phase 1**: 역할 및 권한 테이블 생성
2. **Phase 2**: 기존 `instructor_codes` 데이터 마이그레이션
3. **Phase 3**: 백엔드 미들웨어/데코레이터 구현
4. **Phase 4**: 프론트엔드 메뉴/버튼 권한 적용
5. **Phase 5**: 감사 로그 시스템 추가

---

### 방안 2: **현재 시스템 고도화**
기존 구조를 유지하면서 개선

#### 개선 사항
1. **메뉴별 세부 권한 추가**
```json
{
  "students": {
    "access": true,
    "create": true,
    "update": true,
    "delete": false,
    "export": true
  },
  "courses": {
    "access": true,
    "create": false,
    "update": false,
    "delete": false
  }
}
```

2. **권한 템플릿 시스템**
```javascript
const PERMISSION_TEMPLATES = {
  'admin': { /* 모든 권한 */ },
  'instructor': { /* 강사 권한 */ },
  'viewer': { /* 조회만 */ }
}
```

3. **동적 권한 검증**
```javascript
// 백엔드
@require_permission('students', 'create')
async def create_student():
    pass

// 프론트엔드
if (hasPermission('students', 'update')) {
    showEditButton();
}
```

#### 장점
- ✅ 기존 코드 변경 최소화
- ✅ 빠른 적용 가능
- ✅ 학습 곡선 낮음

#### 단점
- ⚠️ 확장성 제한
- ⚠️ 권한 상속 구조 부족

---

### 방안 3: **하이브리드 접근**
RBAC + 개별 권한 오버라이드

#### 구조
```javascript
{
  "role": "instructor",              // 기본 역할
  "role_permissions": { /* ... */ }, // 역할에서 상속된 권한
  "custom_permissions": {            // 개별 사용자 특별 권한
    "students:delete": true,         // 역할에 없는 권한 추가
    "courses:create": false          // 역할 권한 제한
  }
}
```

#### 장점
- ✅ 유연성과 편의성 균형
- ✅ 특별한 케이스 대응 가능
- ✅ 점진적 마이그레이션 가능

---

## 🎨 UI/UX 개선 제안

### 1. 권한 관리 대시보드
```
┌─────────────────────────────────────┐
│ 🔐 권한 관리                        │
├─────────────────────────────────────┤
│ [역할 관리] [권한 매트릭스] [로그]  │
├─────────────────────────────────────┤
│                                     │
│  역할: 강사 (instructor)            │
│  ├─ 학생 관리                       │
│  │  ├─ ✓ 조회                      │
│  │  ├─ ✓ 수정                      │
│  │  └─ ✗ 삭제                      │
│  ├─ 과정 관리                       │
│  │  └─ ✓ 조회만                    │
│  └─ 상담 관리                       │
│     └─ ✓ 전체                       │
│                                     │
└─────────────────────────────────────┘
```

### 2. 권한 매트릭스 뷰
```
         │ 조회 │ 생성 │ 수정 │ 삭제 │ 내보내기
─────────┼──────┼──────┼──────┼──────┼─────────
학생관리 │  ✓   │  ✓   │  ✓   │  ✗   │   ✓
과정관리 │  ✓   │  ✗   │  ✗   │  ✗   │   ✗
상담관리 │  ✓   │  ✓   │  ✓   │  ✗   │   ✓
```

### 3. 권한 변경 로그
```
┌────────────────────────────────────────┐
│ 📋 권한 변경 이력                      │
├────────────────────────────────────────┤
│ 2026-02-06 14:30                       │
│ 홍길동(IC-001) → 김철수(IC-005)       │
│ 역할 변경: 보조강사 → 강사             │
│ 변경자: 관리자(ADMIN-01)               │
└────────────────────────────────────────┘
```

---

## 🚀 구현 로드맵

### 빠른 개선 (1-2일)
1. **현재 시스템 정리**
   - `permissions`와 `menu_permissions` 통합
   - 권한 검증 함수 표준화
   - 불필요한 코드 제거

2. **기본 CRUD 권한 추가**
   - 메뉴별 create, read, update, delete 권한
   - 프론트엔드 버튼 show/hide

3. **권한 템플릿 적용**
   - 관리자, 강사, 보조강사 프리셋

### 중기 개선 (1주)
1. **RBAC 테이블 구조 생성**
2. **기존 데이터 마이그레이션**
3. **백엔드 권한 검증 미들웨어**
4. **프론트엔드 권한 체크 함수**

### 장기 개선 (2-4주)
1. **권한 관리 UI 페이지**
2. **감사 로그 시스템**
3. **권한 상속 및 오버라이드**
4. **API 레벨 권한 검증 강화**
5. **세션 관리 개선**

---

## 💡 추천 사항

**1순위: 방안 1 (RBAC)** 
- 중장기적으로 가장 확장성 좋음
- 표준 방식으로 유지보수 용이
- 교육 시스템의 복잡한 권한 구조에 적합

**빠른 시작:**
1. 현재 시스템 정리 (1일)
2. RBAC 기본 구조 구축 (2일)
3. 점진적 마이그레이션 (1주)

**시작 시 질문:**
1. 현재 몇 개의 역할(role)이 필요한가요?
2. 메뉴/기능별로 어떤 권한 제어가 필요한가요?
3. 권한 위임 기능이 필요한가요? (예: 관리자가 임시로 권한 부여)
4. 권한 만료 기능이 필요한가요?

---

**어떤 방향으로 진행하시겠습니까?** 🎯
